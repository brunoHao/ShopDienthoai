@model IEnumerable<WEBSITEDIENTHOAI.Models.CTHOADON>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IDhoadon)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ngaythanhtoan)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IDhoadon)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ngaythanhtoan)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDcthoadon">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDcthoadon">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDcthoadon">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
